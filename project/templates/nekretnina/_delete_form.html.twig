<form method="post" action="{{ path('app_nekretnina_delete', {'id': nekretnina.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nekretnina.id) }}">
    <button class="btn">Delete</button>
</form>
